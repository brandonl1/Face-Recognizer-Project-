# Project dependencies for face recognition tool
# The versions below are pinned to ensure compatibility with the tutorial from Real Python.
# You can adjust the versions to match your system requirements, but be aware
# that API changes in newer versions may require code modifications.
dlib==19.24.0
face-recognition==1.3.0
numpy==1.24.2
Pillow==9.4.0